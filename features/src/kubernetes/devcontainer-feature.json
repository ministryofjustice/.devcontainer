{
  "id": "kubernetes",
  "version": "1.1.0",
  "name": "Kubernetes",
  "description": "Installs the Kubernetes CLI, Helm CLI and Helm Chart Testing CLI",
  "options": {
    "kubernetesCliVersion": {
      "type": "string",
      "description": "Version of the Kubernetes CLI to install",
      "proposals": ["latest"],
      "default": "latest"
    },
    "installKubernetesCliPrompt": {
      "type": "boolean",
      "description": "Whether to install the Kubernetes CLI prompt",
      "default": true
    },
    "helmCliVersion": {
      "type": "string",
      "description": "Version of the Helm CLI to install",
      "proposals": ["latest"],
      "default": "latest"
    },
    "helmCtCliVersion": {
      "type": "string",
      "description": "Version of the Helm Chart Testing CLI to install",
      "proposals": ["latest"],
      "default": "latest"
    },
    "veleroCliVersion": {
      "type": "string",
      "description": "Version of the Velero CLI to install",
      "proposals": ["latest"],
      "default": "latest"
    },
    "installVeleroCli": {
      "type": "boolean",
      "description": "Whether to install the Velero CLI",
      "default": false
    }
  },
  "customizations": {
    "vscode": {
      "extensions": ["ms-kubernetes-tools.vscode-kubernetes-tools"]
    }
  }
}
