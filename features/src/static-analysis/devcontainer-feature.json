{
  "id": "static-analysis",
  "version": "2.0.0",
  "name": "Static Analysis",
  "description": "Installs the Checkov CLI",
  "options": {
    "checkovCliVersion": {
      "type": "string",
      "description": "Version of Checkov CLI to install",
      "proposals": ["latest"],
      "default": "latest"
    }
  }
}
